apiVersion: v1
kind: Pod
metadata:
  name: localshops-dev-storage-prep-node01
  namespace: kube-system
  labels:
    app.kubernetes.io/name: localshops-dev-storage-prep
spec:
  automountServiceAccountToken: false
  enableServiceLinks: false
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: node01
  containers:
    - name: prepare
      image: busybox:1.37
      command: ["sh", "-ec"]
      args:
        - |
          install -d -o 999 -g 999 -m 0750 /host/var/lib/localshops-dev/postgres
          stat -c 'prepared %n owner=%u:%g mode=%a' /host/var/lib/localshops-dev/postgres
      securityContext:
        allowPrivilegeEscalation: true
        runAsUser: 0
      volumeMounts:
        - name: host-root
          mountPath: /host
  volumes:
    - name: host-root
      hostPath:
        path: /
        type: Directory
